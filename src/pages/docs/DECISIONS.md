# Pages Architecture Decisions

## 1. Domain Grouping
- Each subfolder (`auth`, `invoices`, `products`, `customers`) houses its own views, sub-components, and documentation.
- Ensures separation of concerns across transactional flows.
